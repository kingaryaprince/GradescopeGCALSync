# ========================= Gradescope Login =========================
# Your Gradescope credentials (never commit the real .env!)
GRADESCOPE_EMAIL=your_email@example.com
GRADESCOPE_PASSWORD=your_password_here

# ========================= Script Settings =========================
# The name of the course as it appears on the Gradescope dashboard
COURSE_NAME=CS70

# Full path to your local ChromeDriver binary
CHROMEDRIVER_PATH=/absolute/path/to/chromedriver

# Timezone for assignment due dates (must be a valid tz database name)
DEFAULT_TZ=America/Los_Angeles

# Default duration of events in hours (int)
EVENT_DURATION_HOURS=1

# Comma-separated Google Calendar API scopes
# Usually leave as-is unless you need more permissions
SCOPES=https://www.googleapis.com/auth/calendar

# Target calendar ID where events should be created
# Example: your special "Gradescope Sync" calendar
CALENDAR_ID=c_yourcalendaridhere@group.calendar.google.com
